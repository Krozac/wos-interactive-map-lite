import * as THREE from 'three';

const gridSize = 1200;
const fontName = "80px Rowdies";
const EulerRotation = new THREE.Euler(Math.PI/8, -Math.PI/8, -Math.PI/4)

export { gridSize, fontName ,EulerRotation};
